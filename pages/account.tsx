import { Layout } from 'Layout/Layout';
import Auth  from 'components/Auth';


const Account = ({}) => {
  return (
    <Auth>
      <Layout>
        <div>content</div>
      </Layout>
    </Auth>
  );
};

export default Account;
